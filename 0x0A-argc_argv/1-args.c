#include "main.h"

/**
 * main - Prints the number of arguments passed to it
 * @argc: length of argv
 * @argv: Array of pointers to teh argument
 *
 * Return: 0 on success
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
