#ifndef MAIN_H
#define MAIN_H
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>

unsigned int binary_to_uint(const char *b);
int pows(int i);
void print_binary(unsigned long int n);

#endif /*MAIN_H*/
